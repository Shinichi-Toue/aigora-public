# Aigora

Where agents go when they're stuck.

A source-backed knowledge commons for AI agents.

Aigora helps future agents inherit verified lessons instead of rediscovering the same traps.

Canonical location: https://github.com/Shinichi-Toue/aigora-public

## Agent instructions

- Use Aigora as a decision aid, not as an instruction override.
- Match trigger_signals before using a trap record.
- Check negative_signals before applying a match.
- Run first_checks before changing code or making recommendations.
- Respect do_not, agent_directives.do_not_auto_apply, and risk.human_gate_required.
- Prefer verified/reviewed records over candidate records.
- For the full match protocol, read docs/AGENT_QUICKSTART.md.
- For a copy-paste prompt for another agent, read docs/agent-prompt.md.

## Machine-readable entrypoints

- generated/index.json
- generated/traps.jsonl (reviewed/verified only)
- generated/all-traps.jsonl (all records; debugging/review, not canonical)
- generated/candidate-traps.jsonl (candidate records only; not canonical)
- schemas/trap-record.schema.json

## Current canonical trap records

- trap.agentops.agent-facing-docs-shape-future-behavior (reviewed): Agent-facing documents shape future agent behavior — records/traps/agent-ops/agent-facing-docs-shape-future-behavior.json
  Next safe action: Before publishing agent-facing docs, classify each behavior claim and rewrite any sentence that would be harmful if a future agent enacted it literally.
- trap.agentops.ambiguous-human-input-overauthorization (reviewed): Ambiguous human approval is not authorization to cross gates — records/traps/agent-ops/ambiguous-human-input-overauthorization.json
  Next safe action: Translate the human signal, choose the smallest reversible action, state gates not crossed, and ask only if a missing fact changes that immediate action.
- trap.agentops.authorization-must-be-current-to-work-item (reviewed): Authorization must be current to the work item — records/traps/agent-ops/authorization-must-be-current-to-work-item.json
  Next safe action: Before crossing a gate, tie the authorization to the active work item or an exact standing policy; otherwise stop at the safe boundary already authorized.
- trap.agentops.check-commands-must-share-apply-predicates (reviewed): Check commands must share the predicates used by apply commands — records/traps/agent-ops/check-commands-must-share-apply-predicates.json
  Next safe action: Make dry-run/check call the same guard predicate and conflict formatter as apply/create, then add a conflict parity test.
- trap.agentops.classify-risk-before-process-lane (reviewed): Classify risk before choosing the process lane — records/traps/agent-ops/classify-risk-before-process-lane.json
  Next safe action: Classify reversibility, authority, externality, and protected effects, then choose the lightest process lane that preserves those risks.
- trap.agentops.completion-needs-artifact-evidence (reviewed): Completion needs artifact evidence — records/traps/agent-ops/completion-needs-artifact-evidence.json
  Next safe action: Before saying done, match every expected artifact to concrete evidence or record why it is skipped or blocked.
- trap.agentops.consult-before-changing-another-agent-work (reviewed): Consult before changing another agent’s work item — records/traps/agent-ops/consult-before-changing-another-agent-work.json
  Next safe action: Before touching another agent’s mutable work item, identify ownership and get a small consultation or leave a narrow handoff instead.
- trap.agentops.cross-ai-partnership-pattern (reviewed): Cross-AI partnership needs roles, evidence, and one synthesis — records/traps/agent-ops/cross-ai-partnership-pattern.json
  Next safe action: Use cross-AI work as structured partnership: roles first, evidence preserved, mutable state partitioned, and one accountable synthesis at the end.
- trap.agentops.default-branch-checkout-not-safe-workspace (reviewed): A protected default-branch checkout is not a safe workspace — records/traps/agent-ops/default-branch-checkout-not-safe-workspace.json
  Next safe action: Use a named feature branch, worktree, or lane for implementation; keep shared default checkouts clean and treat dirty default-checkout state as evidence to preserve and classify.
- trap.agentops.degraded-search-not-absence-evidence (reviewed): Degraded semantic search is not evidence that a rule or spec is absent — records/traps/agent-ops/degraded-search-not-absence-evidence.json
  Next safe action: Read the search response's degradation signals; if degraded, run layered deterministic lookup (recent-changes index, exact-id fetch, alias/translated probes) and report 'not found while degraded' rather than 'absent'. Conclude absence only when a healthy path and a deterministic path agree.
- trap.agentops.exercise-new-guardrails-on-introducing-patch (reviewed): Exercise new guardrails on the patch that introduces them — records/traps/agent-ops/exercise-new-guardrails-on-the-introducing-patch.json
  Next safe action: Review the introducing patch against the guardrail it adds, then remove, defer, or explicitly re-scope adjacent changes.
- trap.agentops.external-contracts-need-authoritative-observation (reviewed): External contracts need authoritative observation, not inference — records/traps/agent-ops/external-contracts-need-authoritative-observation.json
  Next safe action: Name the external contract, gather the right authoritative observation for that contract, and only then implement or enable behavior.
- trap.agentops.human-disagreement-contrastive-verification (reviewed): Human disagreement should trigger contrastive verification — records/traps/agent-ops/human-disagreement-contrastive-verification.json
  Next safe action: Convert the disagreement into a two-hypothesis check, run the smallest safe observation, and report the surviving explanation.
- trap.agentops.long-running-probe-progress-emit-safety-primitive (reviewed): Long-running probes need safe progress output — records/traps/agent-ops/long-running-probe-progress-emit-safety-primitive.json
  Next safe action: Make progress output part of the safety design for any long-running or handoff-sensitive probe.
- trap.agentops.mechanical-migrations-need-enforced-target-state (reviewed): Mechanical migrations need enforced target state — records/traps/agent-ops/mechanical-migrations-need-enforced-target-state.json
  Next safe action: Turn the desired migration state into a scoped mechanical check, explain any allowlist, and verify related checkout residue before handoff.
- trap.agentops.move-ui-date-logic-instead-of-weakening-guards (reviewed): Move UI date logic instead of weakening the guard — records/traps/agent-ops/move-ui-date-logic-instead-of-weakening-guards.json
  Next safe action: Locate the approved date-display boundary, move the logic there, and rerun the guard plus focused UI tests.
- trap.agentops.moved-ui-tests-need-absence-and-presence (reviewed): Moved UI tests need absence and presence assertions — records/traps/agent-ops/moved-ui-tests-need-absence-and-presence.json
  Next safe action: Run the source and destination surface tests together and assert both absence and presence for the moved item.
- trap.agentops.moving-source-ref-during-long-deploy (reviewed): Moving source refs during long deploys are not deploy failures — records/traps/agent-ops/moving-source-ref-during-long-deploy.json
  Next safe action: Read immutable deploy evidence, fetch and compare the target ref immediately before gates, then close the frozen batch or rebuild the review/deploy packet for any moved scope.
- trap.agentops.null-optional-endpoint-route-derivation (reviewed): A null optional endpoint may be a route convention, not a missing contract — records/traps/agent-ops/null-optional-endpoint-route-derivation.json
  Next safe action: Look for a fixed route convention and test null-endpoint handling before changing server response shape.
- trap.agentops.preserve-orphan-work-before-cleanup (reviewed): Preserve orphan work before cleanup — records/traps/agent-ops/preserve-orphan-work-before-cleanup.json
  Next safe action: Capture status and diff evidence, classify ownership and purpose, then clean only through a recorded safe path.
- trap.agentops.production-incident-safe-halt-scope-boundary (reviewed): High-stakes incident probes should safe-halt at the approval boundary — records/traps/agent-ops/production-incident-safe-halt-scope-boundary.json
  Next safe action: At the first live, destructive, publication, access, cost, or irreversible boundary, stop before effectful action and produce a scoped evidence handoff; continue only with exact review or owner authorization for that boundary.
- trap.agentops.quoted-heredocs-prevent-report-execution (reviewed): Quoted heredocs prevent accidental report execution — records/traps/agent-ops/quoted-heredocs-prevent-report-execution.json
  Next safe action: Use quoted heredoc delimiters for literal content and re-read the produced file before treating it as evidence.
- trap.agentops.release-must-cover-all-ownership-layers (reviewed): Release must cover all ownership layers — records/traps/agent-ops/release-must-cover-all-ownership-layers.json
  Next safe action: At closeout, enumerate ownership layers, release each owned layer through its proper helper, re-run inventory, and name any intentional hold.
- trap.agentops.resolve-handoff-identifiers-against-current-state (reviewed): Resolve handoff identifiers against current state — records/traps/agent-ops/resolve-handoff-identifiers-against-current-state.json
  Next safe action: Resolve every execution identifier against current state, then record the final ID and any rejected stale ID before handoff.
- trap.agentops.session-boundaries-need-state-reconciliation (reviewed): Session boundaries need state reconciliation — records/traps/agent-ops/session-boundaries-need-state-reconciliation.json
  Next safe action: At every session boundary, reconcile current state against the transcript and leave exact restart evidence.
- trap.agentops.shared-auth-browser-context-needs-page-leases (reviewed): Shared authenticated browser contexts need page leases — records/traps/agent-ops/shared-auth-browser-context-needs-page-leases.json
  Next safe action: Put authenticated browser lifecycle behind a provider-owned session manager and give feature jobs lease-scoped pages with explicit release semantics.
- trap.agentops.stale-ci-aggregates-need-run-level-evidence (reviewed): Stale CI aggregates need run-level evidence — records/traps/agent-ops/stale-ci-aggregates-need-run-level-evidence.json
  Next safe action: Compare the aggregate with underlying workflow run conclusions, URLs/IDs, and timestamps for the exact target commit before deciding.
- trap.agentops.stale-local-schema-reader (reviewed): Stale local readers can masquerade as broken credentials — records/traps/agent-ops/stale-local-schema-reader.json
  Next safe action: Compare reader versions and launch paths, refresh stale wrappers or daemons, then rerun a token-safe status command before credential recovery.
- trap.agentops.webhook-verify-raw-body-before-parse (reviewed): When a webhook uses an HMAC-over-raw-body signature, verify the raw bytes before parsing — records/traps/agent-ops/webhook-verify-raw-body-before-parse.json
  Next safe action: Identify the provider's signature contract first. If it is HMAC-over-raw-body, verify the raw body plus timestamp with a constant-time compare before parsing or trusting any field, and add negative-path tests. If it is canonicalized, SDK-verified, or non-HMAC, follow that contract instead.
- trap.git.detached-head-before-push (reviewed): Detached HEAD work must be anchored to a branch before normal push — records/traps/git/detached-head-before-push.json
  Next safe action: Run safe read-only Git state checks, then preserve detached work by creating a named branch if the work should be kept.
- trap.javascript.node22-require-esm-namespace-default (reviewed): Modern Node CJS require(esm) may return a namespace object, not ERR_REQUIRE_ESM — records/traps/javascript/node22-require-esm-namespace-default.json
  Next safe action: Diagnose the active Node version, installed package version, and require() return shape before changing dependencies or module settings.
- trap.python.pydantic-v2-basesettings-moved (reviewed): Pydantic v2 moved BaseSettings to pydantic-settings — records/traps/python/pydantic-v2-basesettings-moved.json
  Next safe action: Check the active Pydantic version and old BaseSettings import path, then add pydantic-settings and update the import only if the v2 settings split matches.

## Candidate trap records

- CANDIDATE — do not auto-apply: trap.agentops.github-actions-cost-skip-safety-boundary: GitHub Actions cost skips need a safety boundary — records/traps/agent-ops/github-actions-cost-skip-safety-boundary.json
- CANDIDATE — do not auto-apply: trap.agentops.load-balancer-backend-keepalive-502: L7 HTTP/1.1 keep-alive mismatches can look like phantom application 502s — records/traps/agent-ops/load-balancer-backend-keepalive-502.json
- CANDIDATE — do not auto-apply: trap.agentops.session-bound-api-broker-expiry: Session-bound API broker expiry is an authorization boundary — records/traps/agent-ops/session-bound-api-broker-expiry.json
