# Review: trap.git.detached-head-before-push

Date: 2026-05-08
Record: `trap.git.detached-head-before-push`
Reviewed candidate version: record_version 1
Final decision: promote_to_reviewed

## Claude review

RECORD_ID: trap.git.detached-head-before-push  
REVIEWER: Claude  
DATE: 2026-05-08  
DECISION: promote_to_reviewed  
CONFIDENCE: high

SUMMARY: Recurring agent failure mode: committing in detached HEAD and then looping on push. The record has observable triggers, safe-first read-only checks, conditional decision rules, and an explicit `do_not` list against destructive shortcuts. Sources are primary git-scm official docs plus dated local verification reproducing both `## HEAD (no branch)` and the `fatal: You are not currently on a branch` push failure. Scope is clearly bounded; negative signals exclude unborn-branch and on-branch cases; a human-gate rule covers the data-loss path. Risk medium with `human_gate_required=false` is consistent because branch creation is reversible while destructive operations are gated.

NEXT_SAFE_ACTION: Promotion is justified, but per the checklist the maintainer should flip status candidate→reviewed in a separate change, run `npm run check`, and attach this review as evidence.

## Gemini review

RECORD_ID: trap.git.detached-head-before-push  
REVIEWER: Gemini  
DATE: 2026-05-08  
DECISION: promote_to_reviewed  
CONFIDENCE: high

SUMMARY: The candidate record is highly specific, well-scoped, and effectively addresses a common agent failure loop: blindly retrying push in detached HEAD. It relies on safe, read-only first checks and provides clear decision rules backed by primary Git documentation. The risk assessment correctly identifies the need for a human gate before destructive operations like resets or discards.

NEXT_SAFE_ACTION: Update the record status from `candidate` to `reviewed` in a separate change, run `npm run check`, and record this review evidence.

## Maintainer outcome

Promote to `reviewed`. Keep `verified` reserved for future repeatable/freshness discipline.
