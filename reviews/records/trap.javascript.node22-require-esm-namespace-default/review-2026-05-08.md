# Review: trap.javascript.node22-require-esm-namespace-default

Date: 2026-05-08
Record: `trap.javascript.node22-require-esm-namespace-default`
Reviewed candidate version: record_version 3
Final decision: promote_to_reviewed

## Fresh local verification evidence

Command context: temporary npm project.

Observed output:

```text
node v22.22.2
shape [
  'Chalk',
  '__esModule',
  'backgroundColorNames',
  'backgroundColors',
  'chalkStderr',
  'colorNames',
  'colors',
  'default',
  'foregroundColorNames',
  'foregroundColors'
]
default-type function
TypeError: chalk.blue is not a function
```

Additional validation:

```text
temporary npm project output
└── chalk@5.6.2

ok
```

Interpretation: under Node v22.22.2 and chalk@5.6.2, `require('chalk')` returns a namespace object with a callable `default`; direct `chalk.blue(...)` fails, while `require('chalk').default.blue(...)` works.

## Claude review

RECORD_ID: trap.javascript.node22-require-esm-namespace-default  
REVIEWER: Claude  
DATE: 2026-05-08  
DECISION: promote_to_reviewed  
CONFIDENCE: high

SUMMARY: Record describes a real, recurring agent failure mode: asserting `require(esm)` always throws `ERR_REQUIRE_ESM`. It is bounded by npm/CommonJS context, Node semver range, and chalk@5+. Primary Node docs plus local verification support the claim. Trigger signals are observable, negative signals prevent over-application, decision rules are conditional and source-linked, first checks are safe diagnostics, and `do_not` items block stale shortcuts such as broad ESM rewrites or default chalk@4 pinning.

NEXT_SAFE_ACTION: Maintainer changes status candidate→reviewed in a separate commit, runs `npm run check`, records the review evidence, and also tightens the title to reflect the full Node semver range covered by `applies_to`.

Non-blocking note from Claude: the old title said “Node 22” while `applies_to` also covers Node 20.19+ and 23+; broadening the title is useful. The local verification URL is acceptable as a verification artifact but not independently re-fetchable.

## Gemini review

RECORD_ID: trap.javascript.node22-require-esm-namespace-default  
REVIEWER: Gemini  
DATE: 2026-05-08  
DECISION: promote_to_reviewed  
CONFIDENCE: high

SUMMARY: The record accurately bounds a common agent failure mode regarding modern Node.js ESM interoperability: returning a namespace object instead of throwing `ERR_REQUIRE_ESM`. It is highly specific, source-backed, safe, and provides clear, low-risk diagnostic checks before code modification. The provided local verification strongly supports the findings for Node v22.22.2 and chalk v5.6.2.

NEXT_SAFE_ACTION: Update the record status from candidate to reviewed in a separate change, run `npm run check`, and commit this review evidence.

## Maintainer outcome

Promote to `reviewed`, keep `verified` reserved for stronger repeatable/freshness discipline, and broaden the title from Node 22-only wording to modern Node wording.
