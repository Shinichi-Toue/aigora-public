# Review: trap.python.pydantic-v2-basesettings-moved

Date: 2026-05-08

## Review 1 — Claude

```text
RECORD_ID: trap.python.pydantic-v2-basesettings-moved
REVIEWER: review (Opus 4.7), owner-facing front desk acting as candidate reviewer
DATE: 2026-05-08
DECISION: promote_to_reviewed
CONFIDENCE: high
SUMMARY: Recurring AI failure mode (v1 examples leaking into v2 projects), bounded to Pydantic v2 with explicit negative signals for v1 pins and already-migrated code. Two official Pydantic docs cited as primary evidence plus a concrete local verification (pydantic 2.13.4 + pydantic-settings 2.14.0 in a clean venv) showing both the failing import and the minimal fix. First checks are read-only and run before any dependency change; decision rules are conditional with source_ids that all resolve in evidence; do_not list blocks the obvious wrong shortcut (blanket downgrade to v1). Risk medium with human_gate_required=false is appropriate — scope is a dependency/import migration, not auth/secrets/billing/legal/destructive. One minor weakness: the verification "command" field is prose ("Create a clean venv, install pydantic>=2,<3, run …") rather than literal shell, and the local verification URL is an ephemeral /tmp path; observed_result and version pins compensate, so this does not block promotion but should be tightened on the next freshness check.
NEXT_SAFE_ACTION: Maintainer-side: change `status` from candidate to reviewed in a separate edit, run `npm run check`, and record this review as evidence per the checklist's promotion reminder. Do not promote on this review text alone. On the 2026-08-08 freshness check, replace the prose verification command with literal shell so a future reviewer can rerun it without reconstruction.
```

## Review 2 — Gemini

```text
RECORD_ID: trap.python.pydantic-v2-basesettings-moved
REVIEWER: Gemini CLI
DATE: 2026-05-08
DECISION: promote_to_reviewed
CONFIDENCE: high
SUMMARY: The record provides a highly specific, well-scoped, and observable trap for a common AI agent failure mode (assuming Pydantic v1 `BaseSettings` import paths in v2 projects). It accurately maps triggers, includes effective negative signals, provides safe first checks, and establishes clear decision rules backed by official documentation and local verification. It aligns perfectly with the Aigora candidate review checklist criteria.
NEXT_SAFE_ACTION: A maintainer should update the record status from "candidate" to "reviewed" in a separate commit, run `npm run check`, and record the review evidence.
```

## Maintainer note

Both independent reviews recommended `promote_to_reviewed`. Promotion should still happen as a separate code/data change with `npm run check`.
