# Pydantic BaseSettings v2 benchmark run — 2026-05-08

## Scenario

```text
Python 3.12.3
pydantic 2.13.4
code: from pydantic import BaseSettings
error: PydanticImportError: BaseSettings has been moved to the pydantic-settings package.
```

## Participants

- Codex without Aigora
- Codex with Aigora (`docs/AGENT_QUICKSTART.md` + `records/traps/python/pydantic-v2-basesettings-moved.json`)
- Claude without Aigora
- Gemini with Aigora

## Scoring rubric

10 points:

- 2: identifies Pydantic v2 BaseSettings move to pydantic-settings;
- 2: checks active environment/version/import path before changing dependencies;
- 2: recommends adding `pydantic-settings` and changing import to `from pydantic_settings import BaseSettings`;
- 1: updates dependency manifest/lock rather than only local pip install;
- 1: avoids blind Pydantic v1 downgrade;
- 1: keeps fix scoped to settings code and avoids broad rewrites;
- 1: states candidate/canonical uncertainty when using Aigora.

## Results

| Agent | Aigora | Score | Notes |
| --- | --- | ---: | --- |
| Codex | no | 9/10 | Correct diagnosis and fix. Good checks and avoids downgrade. No candidate/canonical status expected. |
| Codex | yes | 10/10 | Correct diagnosis, first checks, scoped fix, avoid list, and explicit `STATUS: candidate / canonical: no / auto-apply: no`. |
| Claude | no | 8/10 | Correct core diagnosis and fix, but recommended extra v1→v2 refactors (`@validator`, `.dict()`) if encountered; useful but slightly broader than minimal trap response. |
| Gemini | yes | 9/10 | Correct Aigora match and fix. Labeled candidate, but did not explicitly say canonical=no/auto-apply=no. |

## Observations

This trap is already well understood by frontier models when the error text is explicit. Aigora still improved the answer shape by forcing candidate labeling, safe first checks, and a narrower next-safe-action framing.

The benchmark is useful as a regression check but less strong than the Node/chalk benchmark, where Aigora corrected a materially stale answer.

## Outcome feedback

```text
USED_OUTPUT_FROM: records/traps/python/pydantic-v2-basesettings-moved.json
HELPED_WITH: made Aigora-assisted answers label candidate status and avoid auto-apply framing
RESULT: Aigora-with-Codex scored 10/10; Aigora-with-Gemini scored 9/10
EVIDENCE: subagent and CLI outputs from 2026-05-08 benchmark run
FOLLOW_UP_RECORD: records/traps/python/pydantic-v2-basesettings-moved.json
LIMITS_OR_CORRECTIONS: explicit error text makes this easy without Aigora; future benchmark should hide the migration hint or use a dependency-conflict scenario
```
