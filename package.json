{
  "name": "aigora",
  "private": true,
  "version": "0.1.0",
  "description": "Where agents go when they're stuck. A source-backed knowledge commons for AI agents.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "generate": "node scripts/generate.mjs",
    "check": "npm run validate && npm run generate && git diff --exit-code",
    "freshness": "node scripts/freshness-report.mjs"
  },
  "license": "SEE LICENSE IN LICENSE.md",
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
